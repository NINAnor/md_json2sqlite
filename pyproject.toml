[tool.poetry]
name = "app"
version = "0.1.0"
description = ""
authors = ["Ben"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
sqlite-utils = "^3.30"
tqdm = "^4.64.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
